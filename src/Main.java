import java.util.Scanner;

public class Main {

    private static Scanner scanner = new Scanner(System.in);
    public static void main(String[] args) {
        MyNodeList<String> myList = new MyNodeList<>();
        // Tips:
        // .insert(value) - add elements to list
        // .next() - move to next value in list
        // .print() - see all values in list in order they were entered
        // .iterator() - returns an iterator
        //      - use .traverse()
        //      A menu will pop up that will allow user input
        //      you can move backwards, forwards and delete current next node
        
    }
}
